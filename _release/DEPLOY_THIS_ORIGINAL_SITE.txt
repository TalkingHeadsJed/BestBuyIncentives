================================================================================
  ✅ DEPLOY THIS — AUTHORITATIVE PRIMARY DEPLOYMENT SOURCE
  BestBuyIncentives.com — ORIGINAL React SPA design (approved "discounted
  travel voucher" copy). Rebuilt from frontend/ — NOT the static overlay.
================================================================================

PRIMARY DEPLOYMENT SOURCE (use THIS):
  Folder : /app/original-site/          (120 HTML pages, ~99 MB)
  Archive: /app/_release/BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz

  SHA-256 (archive):
  ec3eeb229c1d8647bdbd4421d9d86e4b24f27736129c5d853f9b065bd8dd8731

  Verify before deploying:
    sha256sum BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz
    # must equal the hash above

DO NOT DEPLOY (superseded / non-authoritative):
  /app/static-site/   = the all-91 static OVERLAY (pre-V2). NOT the website design.
  V2 overlay          = parked under git tag `sales-first-v2-local-preserved`.

--------------------------------------------------------------------------------
WHAT'S INSIDE (all pre-rendered, crawlable, hydrate over markup):
  /  /about  /programs  /industries  /case-studies  /faq
  /resources  + 6 article pages under /resources/<slug>
  /articles  (Sales Resources index — CollectionPage, 6 topic filters, links all 72)
  /travel-incentives-vs-discounting  -vs-gift-cards  -vs-cash-rebates
  /contact  /privacy  /terms  /compliance
  + 72 sales-first article pages at root slugs (e.g. /cost-of-delay-in-sales)
  404.html (branded, noindex,follow)
  .htaccess  robots.txt  llms.txt  sitemap.xml (120 URLs)
  static/ (hashed JS+CSS)  images/  downloads/ (4 gated resources: playbook, voucher guide, revenue dashboard, training script)  api/ (first-party PHP consultation + playbook-lead endpoints)
  Audience Lab pixel present on every page (suppressed during prerender only).
  All 120 routes hydrate cleanly (React 19, 0 hydration/#418 errors).
  Articles driven by ONE registry (frontend/src/data/articles.json) — see
  /app/ARTICLE_CONTENT_MANAGEMENT.md for the add/remove + validate workflow.
--------------------------------------------------------------------------------

BLUEHOST EXTRACTION INSTRUCTIONS (exact):

1) BACK UP the current live site first:
     cd ~
     tar czf backup-public_html-$(date +%F).tgz public_html
     cp public_html/.htaccess backup-htaccess-$(date +%F)

2) UPLOAD the archive to your home dir (or public_html) via cPanel File
   Manager or SFTP:
     BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz

3) VERIFY the checksum on the server (cPanel Terminal / SSH):
     sha256sum BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz
     # must equal:
     # ec3eeb229c1d8647bdbd4421d9d86e4b24f27736129c5d853f9b065bd8dd8731

4) EXTRACT. The archive contains a top-level "original-site/" folder, so its
   CONTENTS must land directly in public_html (not in a nested folder):
     mkdir -p ~/_bbi_extract && tar xzf BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz -C ~/_bbi_extract
     # then move the CONTENTS (including hidden files) into public_html:
     shopt -s dotglob
     rm -rf ~/public_html/*            # optional clean deploy (backup taken in step 1)
     cp -a ~/_bbi_extract/original-site/* ~/public_html/
     rm -rf ~/_bbi_extract
   IMPORTANT: confirm the hidden files copied:
     ls -a ~/public_html/.htaccess ~/public_html/404.html

5) FIRST-PARTY PHP APIs (required for the live forms to work):
   - Consultation form: public_html/api/consultation/index.php
   - Playbook gated download (NEW): public_html/api/playbook-lead/index.php
     (with the shared hidden api/.htaccess).
   - Both need the private env vars set OUTSIDE public_html:
       BBI_PRIVATE_STORAGE_DIR (a dir OUTSIDE public_html, chmod 700)
       BBI_RATE_LIMIT_SECRET   (>=32 random chars)
       BBI_SALES_NOTIFICATION_EMAIL (where lead emails go)
       BBI_CRM_WEBHOOK_URL / BBI_CRM_WEBHOOK_TOKEN (optional; enables direct CRM push)
   - Add BOTH 5-minute retry crons (leads are durably queued and retried):
       */5 * * * * php /home/USER/deploy-private/cron/process-consultation-queue.php
       */5 * * * * php /home/USER/deploy-private/cron/process-playbook-queue.php
   - Full steps + secrets + synthetic tests are in:
       /app/_release/BLUEHOST_DEPLOY_CHECKLIST.md
     and the cron workers + env template are in /app/deploy-private/.

6) VERIFY LIVE (after cache purge):
     for u in / /programs/ /industries/ /travel-incentives-vs-discounting/ \
              /contact/ /privacy/ /terms/ /compliance/; do
       echo "$(curl -s -o /dev/null -w '%{http_code}' https://bestbuyincentives.com$u)  $u"; done
     # genuine 404 (must be 404 AND branded, not the homepage):
     curl -s -o /tmp/nf.html -w '%{http_code}\n' https://bestbuyincentives.com/no-such-page-xyz/
     grep -c noindex /tmp/nf.html
     # pixel loads + fires, and one controlled contact submission creates exactly
     # one CRM/queue record (see checklist).

ROLLBACK (if anything fails):
     cd ~ && rm -rf public_html && tar xzf backup-public_html-<DATE>.tgz

--------------------------------------------------------------------------------
NOTE: Nothing has been pushed to GitHub or published. This file marks the
authoritative local artifact only. GitHub reconciliation was NOT performed
(no remote/credentials in this workspace).
================================================================================
