# BestBuyIncentives.com — PRD

## Problem Statement
Replace the old BestBuyIncentives.com site with a modern, bold, sales-focused authority site (Hormozi/Acquisition.com style). Goals: lead-gen, showcasing offerings, booking demos. Audience: B2B business owners & sales leaders. **SEO is the primary traffic channel** — must be indexable by search engines AND AI assistants (ChatGPT, Claude, Perplexity, etc.).

## Hard Requirements
- **100% static site** hostable on standard Apache shared hosting (BlueHost). No Node/npm on server.
- **ZERO Emergent dependencies** — no tracking, PostHog, badges, preview URLs, localhost, or emergentagent CDNs in output.
- Theme: Yellow (#FFD300) / Black / White. NO GREEN. Photo-dominant.
- Client-side ROI calculator, Calendly embed, contact/newsletter forms via Web3Forms.
- Canonical domain: `https://bestbuyincentives.com` (non-www, HTTPS, no trailing slash except root).

## Tech Stack
- React 19 (CRA + craco), Tailwind, framer-motion, react-fast-marquee, sonner, react-helmet-async.
- **Build-time prerendering** (puppeteer-core + system Chrome) → every route ships as real static HTML with hydration.
- Forms: Web3Forms (key hardcoded in `src/lib/api.js`). Video: self-hosted `/public/vsl.mp4`. Images: self-hosted `/public/images`.

## SEO / AI Architecture (added this session)
- `scripts/routes.js` — single source of truth for all 14 routes (shared by prerender + sitemap).
- `scripts/prerender.js` — serves the CRA build, sets `window.__PRERENDER__`, renders each route in headless Chrome, writes `<route>/index.html` with fully-rendered content + per-route Helmet head (title/description/canonical/OG/JSON-LD).
- `scripts/gen-sitemap.js` — regenerates `sitemap.xml` from the route list.
- `src/hooks/useHydrated.js` — returns false during prerender + first client render, true after; used to make client-only/measurement widgets (Marquee, Toaster, Radix Select, Radix Slider, Calendly) render a static version first so hydration is mismatch-free.
- `public/robots.txt` — explicitly allows GPTBot, OAI-SearchBot, ChatGPT-User, ClaudeBot, anthropic-ai, Claude-Web, PerplexityBot, Perplexity-User, Google-Extended, Bingbot, Amazonbot, Applebot(-Extended), CCBot, Meta-ExternalAgent, cohere-ai + sitemap ref.
- `public/llms.txt` — curated Markdown index of the site for LLMs.
- `public/index.html` — global JSON-LD: Organization (legalName, address, contactPoint 866-843-8003, sameAs socials), Service, WebSite. Per-page meta injected by Helmet (deduped — no static per-page tags).
- New `/faq` page (`src/pages/FAQ.jsx`) — answers rendered as always-visible text + FAQPage schema (high value for AI citation). Linked in footer.
- `public/.htaccess` — canonical HTTPS+non-www 301, extensionless→`/index.html` serving (DirectorySlash Off), SPA fallback, HTML no-cache, host-neutral (no Pair.com/BlueHost names).

## Build & Deploy
Build pipeline (container only): `yarn build && node scripts/gen-sitemap.js && node scripts/prerender.js` → output synced to `/app/static-site/`.
Deploy: "Save to GitHub" → upload **contents of `/app/static-site/`** (incl. hidden `.htaccess`) to `public_html`.
Web3Forms: add live domain to "Allowed Domains" so forms send.

## Verification (this session)
- All 14 routes: unique title/description/canonical, real `<h1>`+body+footer in raw HTML (no JS needed) — acceptance test passes via curl-equivalent.
- All 14 routes hydrate with **0 React errors** (verified in headless Chrome).
- No emergentagent/posthog/localhost strings. ROI calc, sliders, marquee, animations unchanged (no visual change).

## Changelog
- 2026-06 (earlier): Static conversion, Web3Forms, phone 866-843-8003, localized images, scrubbed Emergent.
- 2026-06 (this session): Final Vimeo VSL (user's compressed 16.6MB 720p), new Greece testimonial image, FAQ copy change, video-badge "Member Access", full SEO prerendering + AI optimization (above), renamed output folder to `static-site`.
- 2026-06 (this session, cont.): Expanded `/faq` to 20 buyer/AI-style questions in 6 categories (`FAQ_PAGE` in content.js) — answers visible + full FAQPage JSON-LD (20 questions) for answer-engine citation. Calendly embed URL left untouched (`calendly.com/bestbuyincentives5/30min`).
- 2026-06 (this session, cont.): Added visible "FAQ" link to top nav (NAV_LINKS). Built new comparison page `/travel-incentives-vs-discounting` (`src/pages/Comparison.jsx`) — head-to-head table + FAQPage schema + breadcrumb, linked in footer, prerendered, added to sitemap (now 15 routes). Verified by testing agent (frontend, 100% pass, iteration_4.json) with 0 issues; hydration clean across all routes.
- 2026-06 (this session, cont.): Added "Compare" dropdown in top nav (desktop hover + mobile section, `Header.jsx`) and 2 more data-driven comparison pages via `COMPARISONS` map in `src/data/comparisons.js`: `/travel-incentives-vs-gift-cards` and `/travel-incentives-vs-cash-rebates`. All 3 pages prerendered; sitemap now 17 routes. Fixed recurring hydration #418 by wrapping mixed text/expression nodes as single template-literal expressions (`{` ${altLabel}`}`, `{`Travel incentives ${c.label}`}`). Added `scripts/hydration-check.js` (local puppeteer console check) — all routes CLEAN. Rebuilt + prerendered + synced to `/app/static-site` (17 routes, 99M incl. 77M images + 16M vsl.mp4). Verified by testing agent (frontend, 100% pass, iteration_5.json) — 0 issues, 0 console/hydration errors. READY FOR BLUEHOST DELIVERY.
- 2026-07-22: Fixed live `.htaccess` 403 bug on all extensionless routes. Root cause: `DirectorySlash Off` + prerender creating real directories (about/, programs/…) meant the "serve prerendered index" rule (`!-d`) skipped them → Apache 403. Baked the client-provided fix into `frontend/public/.htaccess` (source template) AND `static-site/.htaccess`: a rule placed right after the HTTPS/non-www canonical redirect that serves `/$1/index.html` for a real-directory hit WITHOUT redirecting (keeps no-trailing-slash canonical URL). Now survives future rebuilds + "Save to GitHub".
- 2026-07-22 (content pass): (1) Replaced the flagship certificate flyer with client's new "8 Day & 7 Night Luxury Getaway for 4" banner (`public/images/luxury-getaway-ad.png`, referenced via PROGRAMS[0].image; container aspect changed 4:3→16:10 in ProgramsShowcase + Programs page so the banner text isn't cropped). (2) ROI heading changed to "What can incentives do to your top line?" (`ROICalculator.jsx`). (3) Industries: removed Flooring + Mattress, added E-Commerce Retailers, Travel Clubs, Health Clubs, Employee Rewards (`content.js` INDUSTRIES + new self-hosted photos in `public/images/unsplash/` + `images.js` + `IndustriesGrid.jsx` map + `Industries.jsx` SEO desc + Contact form `INDUSTRIES_OPTS`). (4) Contact form field label "Industry" → "Pick your vertical" (placeholder now "Select one"). Rebuilt + prerendered (17 routes) + synced to `/app/static-site`; verified baked HTML for all changes. READY FOR REDEPLOY.
- 2026-07-22 (Pixel): Added Audience Lab (idpixel) analytics `<script>` to `public/index.html` head; blocked it in `prerender.js` so it doesn't fire from the build container. Verified live: loads 200 + collector 204.
- 2026-07-23 (Priority-13 / All-91 Revenue Release — LOCAL ONLY, not pushed/deployed): Major architecture shift. Verified SHA-256 of 4 client ZIPs (all match). Merged the reconstructed React conversion patch over `src/` (Web3Forms fully removed; new `/api/consultation` contact flow + attribution + analytics events + `/privacy` `/terms` `/compliance` legal pages). Model (A): the **91 prebuilt static overlay pages are now the primary site**; React retained ONLY for `/contact` + legal (prerender route list trimmed in `scripts/routes.js`). Assembled hybrid `/app/static-site` (96 HTML pages, 86MB) via reproducible `/app/_release/assemble.py`: overlay base + React static bundle + contact/legal + images + PHP API into `/api` + pixel injected into all 96 pages + corrected `.htaccess` + CTA-leak fix. Rewrote `Header.jsx`/`Footer.jsx` on the React pages to plain `<a>` hard-nav to valid routes only (removed 404 links + Compare dropdown). New `.htaccess`: genuine 404 (no SPA fallback), `404.html` noindex, dir-serve index.html/index.php without redirect, 10 legacy 301s, HTTPS/non-www. Validation: 95/95 routes, 95 unique titles, 0 issues (`_release/validation-report.json`). Audits: 0 Web3Forms/secrets, 0 prohibited legacy terms, approved "discounted travel voucher" on all, 0 editorial leaks (fixed 18 `Primary/Secondary CTA:` labels → real buttons). PHP consultation API + cron packaged (`deploy-private/` outside public_html). Evidence: `_release/RELEASE_REPORT.md`, `_release/BLUEHOST_DEPLOY_CHECKLIST.md`, `_release/GITHUB_RECONCILIATION.md`, screenshots in `_release/screenshots/`. NOT verified (require Bluehost): live PHP/CRM/idempotency/spam/retry/SLA/PII, live Apache 404/301. OPEN: reconcile newer GitHub remote blogs (no remote/creds in workspace) before any Save to GitHub — see GITHUB_RECONCILIATION.md. Safety refs: tag `pre-p13-local-HEAD`, branch `backup/pre-p13-local`, tarballs in `_release/backup/`.
- 2026-08-06 (ORIGINAL DESIGN RESTORE — user correction: overlay changed presentation beyond "add articles" scope): Rebuilt the original React SPA presentation from `frontend/` (NOT `static-site`). Reference = tag `pre-p13-local-HEAD` (37844e2). Current `src` already held all original design pages; only 3 hybrid-trimmed files restored to full-SPA: `Header.jsx`+`Footer.jsx` (full nav Programs/Industries/Case Studies/Resources/FAQ/About/Compare), `scripts/routes.js` (all 17 original routes + privacy/terms/compliance). Wrote React-SPA `.htaccess` (HTTPS/non-www canonical, dir-serve index.html/index.php, genuine 404, no catch-all-to-home; dropped overlay legacy 301s since every route is a real React page). Added branded noindex `public/404.html` (pixel-carrying). Built full React app → 20 routes prerendered → artifact `/app/original-site` (21 HTML) + PHP `/api/consultation`. Verified: original bold black/yellow React design (screenshots `orig-*`, 0 page errors), all routes 200, canonical + 4 JSON-LD on home, pixel on all 21 pages, Web3Forms 0 refs, consultation API wired, genuine 404, sitemap 20 URLs. Deliverable `BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz` = fe725b1f69185e10da9603b53db5f3019eb8d79379422404231a4f75d049188d. Evidence: `_release/ORIGINAL_DESIGN_RESTORE_REPORT.md`. Copy uses approved "discounted travel voucher" terminology (design/layout is the original; can revert copy to pre-p13 on request). Preserved: `static-site`=pre-V2 overlay untouched; V2 under `sales-first-v2-local-preserved`. Safety tag `pre-original-restore-preserved`. NOT done: Save to GitHub / push / publish / deploy; GitHub reconciliation (no remote/creds); Bluehost live gates.
- 2026-08-06 (V2 RESTORE — user request; V2 overlay changed article presentation beyond the "add articles" scope): Restored the LOCAL workspace to exact pre-V2 state. Preserved V2 first (tag `sales-first-v2-local-preserved` @746a938, snapshot `_release/backup/static-site-V2-preserved-*.tgz`, reports retained). Verified pre-V2 backups existed (tag `pre-v2-local-HEAD` @369b8ba, tarball `static-site-pre-v2-1786079831.tgz`, 281 entries valid). No tracked `frontend/` source had changed for V2, so restore = re-extract `static-site` from the pre-V2 tarball + revert `_release/assemble.py`/`validate.py` (OVERLAY→all91). Proof: full-tree `diff -rq` static-site == pre-V2 tarball IDENTICAL; changed article `cost-of-delay-in-sales` hash matches pre-V2 (`fe0eefa9…`) and differs from V2 (`032d1bfa…`). Preserved elements verified intact: React /contact+/privacy+/terms+/compliance, /api/consultation PHP, pixel on all 96 pages, genuine Apache 404 + noindex 404.html, 11 R=301 redirects, no SPA fallback. Screenshots `_release/screenshots/restore-*`. No Save to GitHub / push / publish / deploy. No article content applied.
- 2026-08-06 (SALES-FIRST V2 — LOCAL ONLY, not pushed/deployed): Verified `...SALES-FIRST-V2-2026-08-06.zip` SHA-256 (MATCH). Sales-forward content republish of the same 91 overlay routes (0 added / 0 removed / 91 replaced in place; 2 improved meta descriptions: `/evaluate-discounted-travel-voucher`, `/explain-discounted-travel-voucher-to-customer`). Integrated by pointing the one-step assembly overlay source to `_release/v2` and rebuilding — re-layers React `/contact`+legal, `/api/consultation` PHP package, Audience Lab pixel (all 96 pages, suppressed in prerender), corrected `.htaccess`, nav/footer fixes, CTA-leak fix, images, downloads (added one-page guide PDF + revenue dashboard xlsx + training script txt). Videos untouched. QA: 95/95 routes, 0 issues; sales-content gate 91/91 (product + benefit bridge + consultation path, 0 prohibited terms); JSON-LD/canonical/sitemap/redirect/genuine-404 pass; responsive render 0 errors. Deliverable `BestBuyIncentives-static-site-V2-DELIVERABLE.tgz` = 37088148ca6d9dbfbd5ccbddc980779456a887e741f4d104f2ea48f40a0f52b5. Evidence: `_release/V2_INTEGRATION_REPORT.md`, `validation-report.json`, `sales-content-audit.json`, screenshots `v2-*`. Backups: tag `pre-v2-local-HEAD`, branch `backup/pre-v2-local`. NOT done: Save to GitHub / push / publish / deploy; GitHub remote reconciliation; Bluehost live gates.
## Backlog
- 2026-08-07 (SEO IMPLEMENTATION from 5 authoritative spec files — verified in original-site; no GitHub/deploy): Implemented commercial intent owners, topic clusters/hubs, the 539-action contextual internal link map, author/reviewer/date + Article entity schema upgrades, publisher logo, metadata + title corrections, and validation gates — all in frontend/ source, rebuilt into original-site (116 routes). Work stayed within authorized SEO additions; existing page layouts/copy unchanged. NEW pages (23): 6 topic hubs `/articles/*`, 7 commercial owners (/sales-closing-incentives, /discounted-travel-vouchers-for-sales, /how-it-works, /high-ticket-sales-incentives, /customer-incentive-programs, /increase-sales-without-discounting, /customer-incentive-comparisons), 8 verticals (7 `/industries/*` + `/partners/marketing-agencies`), 2 `/case-studies/*` (reuse existing case data). Registry now single source: articles.json gained `hub`,`datePublished`=`dateModified`=2026-08-07 (real repo authoring date),`seoTitle` (47 long titles shortened; 3 from checklist verbatim); seoPages.json drives the 23 pages' routes/prerender/sitemap. Contextual links: 440 inserted as natural in-sentence prose at exact CSV section contexts + 72 conversion_cta reuse existing CTA + 27 collapsed by max-2-per-target/dedupe guardrails, 0 unmatched, all 96 targets 200+self-canonical. Schema: site-wide Organization+WebSite @id nodes (Seo.jsx); Article schema with author=Best Buy Incentives Editorial Team, reviewedBy=Karl Kramer CEO, dates, isPartOf, about(cluster), image ImageObject 1200x630, publisher @id→real logo.svg; visible byline + accountability block on all 72; hubs=CollectionPage, commercial/industry=WebPage, all with BreadcrumbList. Task1 metadata fix: resource title → "Stop Discounting, Start Closing: Sales Playbook" (also cleared the pre-existing dup-title). Task6: no FAQPage on new pages (none authored); pre-existing /faq + comparisons keep valid FAQ schema (have visible FAQs), unmodified. VALIDATION: scripts/validate-articles.js = ALL PASS (116 routes: full HTML, unique title/canonical/1H1/schema, ZERO broken links, sitemap 116/116, ZERO banned wording, article schema+byline, org @id every page, ZERO orphans min-inbound 4). scripts/hydration-articles.js = ALL 116 CLEAN (0 #418/console errors — fixed recurring adjacent-text patterns in byline/Cta/hub-button/industry-H2). Deliverable original-site (116 pages + PHP /api + logo) repackaged; archive SHA-256=d00f6f742be558207db7044e297cac01a5a67357bf7742f985ab0cf55776abe4 (~99MB); report _release/SEO_IMPLEMENTATION_REPORT_2026-08-07.md. BLOCKERS (nothing invented): commercial-pages/ drafts, youtube embed manifest, per-article 1200x630 images, and external deployment-date records were NOT provided; 8th commercial page /high-ticket-closing-playbook not created (no draft + would be orphan). Tooling: _release/enrich_articles.py, _release/insert_links.py. Did NOT touch GitHub or deploy.

- 2026-08-09 (8th/FINAL commercial page `/high-ticket-closing-playbook` — verified; no GitHub/deploy): Completed the one remaining planned commercial page from the SEO package. Created it in the restored black/yellow React design as a `WebPage` commercial owner via the existing `CommercialPage` template, using the approved `commercial-pages/` playbook draft (source copy = client overlay `_release/all91/high-ticket-closing-playbook/index.html`; adapted into the seoPages.json section format). Did NOT reproduce the draft's `/downloads/*.pdf` link (asset absent in restored site → would fail broken-link gate); playbook contents presented on-page instead. No facts/stats invented; uses approved "discounted travel voucher" terminology. Registry/sitemap/schema/related wiring is auto-derived from `seoPages.json`→`commercial[]` (routes.js SEO_PATHS, App.js, gen-sitemap, prerender). Schema: WebPage @id + BreadcrumbList + site-wide Organization/WebSite @id; NO Article/FAQPage/VideoObject. NOT an orphan — natural inbound links added from: most relevant hub `/articles/sales-closing` (new optional `cfg.resource` link rendered in Hub header via small SeoPages.jsx branch), commercial page `/sales-closing-incentives` (added to proof), and 6 relevant articles (contextual in-sentence prose after first paragraph: high-ticket-closing-process, sales-closing-techniques-high-ticket-purchases, objection-handling-frameworks-for-sales-managers, price-objection-scripts-high-ticket-sales, close-more-sales-without-lowering-price, sales-manager-stalled-deals-playbook) via idempotent `_release/insert_playbook_links.js`. Rebuilt: normalize-bodyhtml (0 changed) → yarn build → gen-sitemap (117 URLs) → prerender (117 routes). GATES: validate-articles.js = ALL PASS (117 routes, 0 broken links, 0 orphans, 0 banned wording, sitemap 117/117); hydration-articles.js = ALL 117 CLEAN (0 #418). Deliverable original-site regenerated (117 pages + PHP /api preserved); archive `BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz` SHA-256=`e4df412dc5008a907da1340e308d8fc8d21ddb4461ce4c17e1f72f117d5fbe09` (~99MB, 117 index.html); DEPLOY_THIS_ORIGINAL_SITE.txt updated (hash + 116→117). Report: `_release/SEO_IMPLEMENTATION_REPORT_2026-08-09_PLAYBOOK.md`. BLOCKER (nothing invented): the YouTube manifest `youtube/batch-01/YOUTUBE_BATCH_01_EMBED_MANIFEST.csv` (all youtube_url blank) was NOT present in the workspace/surfaced assets, so its rows cannot be stored/mapped without fabricating the video→page mapping — no embeds/VideoObject added; awaiting the actual CSV. No GitHub op; not deployed.

- 2026-08-09 (CORRECTION to the above — real download CTA added; verified; no GitHub/deploy): Per user correction, the playbook page now includes the REAL existing downloadable asset instead of on-page-only contents. Copied the genuine project PDF `_release/all91/downloads/BestBuyIncentives_High-Ticket_Closing_Playbook.pdf` (12,772 bytes, SHA-256 f70ba02d…, matches download-manifest.json) into `frontend/public/downloads/` → ships in build + `original-site/downloads/`. Added an optional `download` field to the playbook's `seoPages.json` commercial config and a secondary **download CTA** (`data-testid="commercial-download-cta"`, plain `<a href download>`) in `CommercialPage` (imports `Download` from lucide-react), alongside the existing consultation CTA. Verified PDF served over preview: `200 application/pdf 12772`. All inbound links from the prior build retained (hub + /sales-closing-incentives proof + 6 articles). Rebuilt: yarn build → gen-sitemap (117) → prerender (117). GATES: validate-articles.js = ALL PASS (117 routes, 0 broken links incl. the /downloads/*.pdf target, 0 orphans, sitemap 117/117); hydration-articles.js = ALL 117 CLEAN; visual screenshot OK. NEW deliverable archive SHA-256=`bfd8017e6dde293e05936e84a96bd79c82cbeffb2bede54a6716cc2031c87fd6` (~99MB, 117 index.html, now includes /downloads + PHP /api); DEPLOY_THIS_ORIGINAL_SITE.txt updated (new hash, /downloads noted). Report: `_release/SEO_IMPLEMENTATION_REPORT_2026-08-09_PLAYBOOK.md`. YouTube manifest still pending its CSV (unchanged; not part of this correction). No GitHub op; not deployed.

- 2026-08-09 (REVENUE UPGRADE — `/high-ticket-closing-playbook` turned into a GATED lead-gen download; verified; no GitHub/deploy): The playbook download is now gated behind a lead form (valid BUSINESS email + first/last name + company required; role/phone/team-size/use-case optional). Download link is revealed ONLY after a durable HTTP 202 from a first-party same-origin endpoint `POST /api/playbook-lead` (idempotent on submission_id + Idempotency-Key; attribution preserved: landing page, original+current utm source/medium/campaign/content/term, referrer, first_seen; playbook_asset_id). NEW files: `frontend/src/pages/PlaybookLanding.jsx` (gated page, App.js routes the path here instead of CommercialPage), `frontend/src/components/site/PlaybookGate.jsx` (form/validation/submit/events/reveal), `frontend/src/lib/playbookClient.js` (business-email denylist + payload builder), `submitPlaybookLead` in `frontend/src/lib/api.js`. Endpoint implemented TWICE with the SAME contract: FastAPI `backend/server.py POST /api/playbook-lead` (Mongo `playbook_leads`, unique index, robust same-origin+preview-suffix guard, 405 handler) for PREVIEW testing, and production PHP `original-site/api/playbook-lead/index.php` (mirrors consultation: origin/method/size guards, HMAC rate-limit, honeypot, atomic durable record outside public_html, sales email, optional CRM webhook) + retry cron `deploy-private/cron/process-playbook-queue.php`. Contract: 202 accepted{download_url}; 422 validation_failed(field_errors)/spam_rejected; 405 method_not_allowed; 400 bad idempotency; 403 origin_rejected; 429 rate_limited; 503 delivery_unavailable(retryable). Privacy-safe analytics (no PII): playbook_form_start, playbook_form_error, playbook_lead_accepted (only after 202), playbook_download_click (on click). Consent line links to /privacy. 2nd commercial inbound link added (`/high-ticket-sales-incentives` proof); prior hub + `/sales-closing-incentives` + 6 article inbound links retained. Fixed a #418 in the consent line ({" "}/adjacent nodes) + an entity double-escape. GATES (all pass): validate-articles.js ALL PASS (117, 0 broken links, 0 orphans, sitemap 117/117, gated page has NO pdf link in prerender), hydration-articles.js ALL 117 CLEAN, true-404 OK, backend contract verified via curl (202/422/405/400/403/no-origin-202/idempotent-1-record) + testing_agent iteration_7 (frontend 100%, backend 100%; PDF resolves 200 application/pdf; dataLayer event order + zero-PII verified). NEW archive `BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz` SHA-256=`a5df74a3089bbe9285bdcd2471f6f24ea7e1c059039c1005d448fae9f065b0a6` (~99MB, 117 index.html, incl. PHP consultation+playbook-lead + /downloads). DEPLOY doc updated (new hash, both endpoints, both crons, env vars). Report: `_release/SEO_IMPLEMENTATION_REPORT_2026-08-09_PLAYBOOK.md`. No GitHub op; not deployed. NOTE: gated PDF is still served statically at /downloads (gate captures the lead in front of it — standard gated-content UX). YouTube manifest still pending its CSV.
- 2026-08-11 (YouTube video embeds + VideoObject on 38 articles; verified; no GitHub/deploy): Received the now-POPULATED manifest `YOUTUBE_FIRST_39_ARTICLE_EMBED_CONTROL.csv` (earlier blocker resolved — URLs present). Embedded each mapped video into its corresponding article and added VideoObject JSON-LD. Video 25 (`customer-incentive-ideas-to-close-sales`, NOT FOUND/blank) intentionally SKIPPED. Added a `video` field (verbatim youtube_video_id + ISO-8601 duration) to 38 articles in articles.json via idempotent `_release/attach_youtube_videos.js`; `Article.jsx` renders a responsive lazy `youtube-nocookie.com/embed/<id>?rel=0` player (16:9, `data-testid=article-video`) at the top of the body and emits `videoObjectSchema()` (name=article title, description=article description, duration=sheet, embedUrl/contentUrl=video, thumbnailUrl=i.ytimg hqdefault, uploadDate=2026-08-11 user-approved, publisher=Org @id — nothing invented). Fixed a figcaption `{expr}` adjacency to avoid #418. GATES: validate-articles.js ALL PASS (117, 1 canonical/page, 0 broken links, 0 orphans, sitemap 117/117); hydration-articles.js ALL 117 CLEAN (gate ignores 3rd-party YT console noise); VideoObject on exactly 38 pages, 0 on video-25/non-video pages; visual confirmed. NEW archive SHA-256=`2619f26c1891bbcb36b3b6acd18c7911d449572c5ed2237d80be3e49754dea9d` (~99MB, 117 index.html). DEPLOY doc hash bumped. Report: `_release/YOUTUBE_EMBED_IMPLEMENTATION_2026-08-11.md`. No GitHub op; not deployed.
- 2026-08-11 (Play badges + 3 MORE gated lead-gen downloads; verified; no GitHub/deploy): (1) Article cards that have a video now show a yellow "▶ VIDEO" pill (Articles.jsx ArticleCard + SeoPages.jsx Hub child cards; data-testid=article-video-badge-<slug>). (2) Gated the other 3 existing resources the same way as the playbook — new routes `/discounted-travel-voucher-guide` (discounted-travel-voucher-one-page-guide → PDF), `/incentive-revenue-dashboard` (revenue-dashboard → XLSX), `/sales-team-training-script` (sales-team-training-script → TXT). Each requires a valid business email before revealing the download via the same durable `POST /api/playbook-lead`. Endpoint GENERALISED with a server-side asset registry (FastAPI `_ASSET_DOWNLOADS` + PHP `ASSET_DOWNLOADS`): client can only unlock a known asset_id (unknown → 422 field_errors.playbook_asset_id); 202 returns the correct download_url per asset. Reused components: PlaybookGate now takes a `copy` prop (playbook copy = defaults); new `GatedResourceLanding.jsx` renders any commercial entry with a `gate` object; App.js routes playbook→PlaybookLanding, other .gate→GatedResourceLanding. 3 asset files copied to public/downloads (4 total). Not orphaned: inbound from hubs (travel-voucher-deployment/measurement-roi/customer-incentive-strategy resource links), playbook proof, sibling pages, + 1 article body link each. Scripts: `_release/register_gated_resources.js`, `_release/link_gated_resources_from_articles.js`. ROUTES now 120. GATES: validate-articles.js ALL PASS (120, 1 canonical/page, 0 broken links, 0 orphans, sitemap 120/120, gated pages have NO pdf link in prerender); hydration-articles.js ALL 120 CLEAN; backend curl (revenue-dashboard→202 XLSX, unknown→422); visual (badges on /articles + gated dashboard page). NEW archive SHA-256=`c096d15d01b029408a85b46938b29bfc7e8faf8d25a895cc241e58535033e364` (~99MB, 120 index.html, PHP consultation+playbook-lead + /downloads[4]). DEPLOY doc updated (hash, 117→120, 4 assets). Report: `_release/GATED_RESOURCES_AND_BADGES_2026-08-11.md`. No GitHub op; not deployed. NOTE: analytics events remain named playbook_* across all gated resources (asset_id distinguishes them).
- 2026-08-12 (Hero autoplay fix + api/.user.ini; verified; no GitHub/deploy): (1) FIX hero video not autoplaying on the live static site — React never serializes the `muted` boolean attr, so prerendered HTML had `<video autoplay>` without `muted` → browsers blocked autoplay. Added a durable post-prerender transform in `frontend/scripts/prerender.js` that re-injects `muted` into any `<video>` with `autoplay` and no `muted`; plus a ref+useEffect in `frontend/src/components/home/Hero.jsx` on both autoplay videos (hero-bg + VSL preview) setting v.muted=true and v.play().catch(). Verified: emitted hero-bg tag now ends `...autoplay="" loop="" playsinline="" preload="auto" muted>`; 0 autoplay videos lack muted; user-triggered modal video (no autoplay) untouched. (2) Shipped `original-site/api/.user.ini` with EXACT content `auto_prepend_file="/home1/sybxwpmy/bbi-private/bbi-env.php"` so PHP-FPM loads the private env bootstrap (BBI_* secrets outside webroot) before every /api script — fixes 503s from missing config. Secret NOT in repo (only the path is). Hardened `original-site/api/.htaccess` to deny serving `.user.ini`. Dotfiles preserved by the build→original-site sync (cp -r api/. api/). GATES: validate-articles.js ALL PASS (120); hydration-articles.js ALL 120 CLEAN; acceptance greps pass; `.user.ini` present in tarball with exact content. NEW archive SHA-256=`ec3eeb229c1d8647bdbd4421d9d86e4b24f27736129c5d853f9b065bd8dd8731` (~99MB, 120 index.html). DEPLOY doc hash bumped. Report: `_release/HERO_AUTOPLAY_AND_USERINI_2026-08-12.md`. No GitHub op; not deployed.


- 2026-08-07 (BASELINE GATE + MAINTAINABLE ARTICLE SYSTEM — verified; GitHub push authorized by user): (0) BASELINE GATE: verified restored site vs authoritative pre-redesign tag `pre-p13-local-HEAD` (37844e2). All design/layout files byte-identical (absent from git diff). Found + fixed contamination: `Footer.jsx` had drifted to overlay/V2-era content with BROKEN links (`/how-it-works/`, `/sales-closing-incentives/`, `/customer-incentive-programs/`, `/customer-incentive-comparisons/`, `/downloads/*.pdf`) and had dropped the newsletter form/Programs column — restored to EXACT pre-p13 footer + 2 authorized deltas (legal links→real routes, one "Sales Resources"→/articles link). All other vs-pre-p13 diffs are prior-approved (consultation PHP API, legal pages, "discounted travel voucher" terminology which also removes all "free vacation" wording). (1) REGISTRY: `frontend/src/data/articles.json` is now the single source of truth (added `category`+`excerpt`, dropped hardcoded `related`); `src/data/articles.js` wraps it (ARTICLES, ARTICLE_SLUGS, CATEGORIES, getRelated). Everything derives: React routes (App.js maps ARTICLE_SLUGS), prerender+sitemap (`scripts/routes.js` requires the JSON), `/articles` index, related links (getRelated → same category first, deterministic). (2) NAV: added "Sales Resources"→/articles to desktop+mobile via `NAV_LINKS` data (Header.jsx unchanged from pre-p13) + one footer link. (3) NEW `/articles` index (`src/pages/Articles.jsx`): black/yellow design, one H1, 6 topic filters, 72 cards grouped by category w/ excerpts, consultation CTA, canonical, CollectionPage+BreadcrumbList JSON-LD, server-prerendered. (4) `Article.jsx`: visible breadcrumb (Home›Sales Resources›Category), registry-computed related, CTAs; Article+BreadcrumbList schema; transcripts already stripped. (5) Docs: `/app/ARTICLE_CONTENT_MANAGEMENT.md` (add/remove workflow, validate+build commands); updated `_release/gen_articles.py` (emits registry JSON + category/excerpt) and `scripts/normalize-bodyhtml.js` (operates on JSON). (6) BUILD+VALIDATE: 93 routes prerendered, sitemap 93 URLs; `scripts/validate-articles.js` = ALL PASS (72/72 + /articles full HTML, unique title/canonical/1×H1/schema, ZERO broken internal links, full sitemap coverage, ZERO banned wording); `scripts/hydration-articles.js` = ALL 93 CLEAN (0 #418). One PRE-EXISTING legacy dup-title (/ vs /resources/stop-discounting-start-closing) surfaced as warning (out of scope, both unchanged). (7) PROOF pre-existing pages unchanged: `<main>` of all 16 pre-existing routes byte-identical to prior deliverable (contact allowing permitted UUID). Testing agent (iteration_6.json) frontend 100%, 0 issues, 0 console/hydration errors. (8) Deliverable `/app/original-site` (93 pages + PHP /api) repackaged; archive `BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz` SHA-256 = `065dc99e5ffd3e38d0b761d1d3cb1e999b968c63637a59f804cd951b78b268b8`; report `_release/ARTICLE_INTEGRATION_REPORT.md`. GitHub: user AUTHORIZED push; NO git remote in workspace so remote reconciliation can't run here — push via Emergent "Save to GitHub" button; nothing auto-pushed. NOT deployed to Bluehost. Hydration rule learned: never place `{expr}` adjacent to literal text or another `{expr}` (wrap in <span> or collapse to one template literal) — caused #418 on /articles; now fixed.

- 2026-08-07 (ORIGINAL-DESIGN-WITH-72-ARTICLES — hydration #418 FIXED, deliverable repackaged; LOCAL): Completed the "add 72 sales-first articles as content-only React routes over the original SPA design" task. Fixed the recurring React 19 hydration #418 on all 72 `/pages/Article.jsx` routes. THREE root causes, all fixed at the data/component layer (no presentation change): (1) HTML entities in plain-text SEO fields (`title`/`description`/`h1`) rendered as React text and double-escaped on prerender (`&amp;#39;`) — decoded via `html.unescape` (loop) in `_release/gen_articles.py` + one-off fix on `src/data/articles.js`; (2) `bodyHtml` entities (`&#39;`) differed from browser-serialized prerender — normalized every `bodyHtml` to the browser's own parse+serialize output via new `scripts/normalize-bodyhtml.js` (puppeteer round-trip, byte-identical to prerender); (3) THE actual #418 trigger: adjacent text nodes in the related-links JSX `<ArrowRight/> {r.label}` (icon + space textnode + label textnode). Puppeteer captures React's *client-render* DOM which lacks SSR comment-separators, so the browser merges `" "+label` into one node on re-parse and hydration can't align them. Fixed by `<ArrowRight/><span>{r.label}</span>` (relies on existing `gap-2`; no literal space). Also moved the article `<style>` block out of `Article.jsx` into global `src/index.css` (React 19 treats inline `<style>` as a hoistable resource — extra hydration risk). Diagnosis method: temporarily disabled the `@emergentbase/visual-edits` babel plugin (adds `x-*` attrs that pollute every dev hydration diff) + injected prerendered `#root` markup into dev `index.html` to read React 19's clean dev mismatch message. Verified: `yarn build` → `node scripts/prerender.js` (92 routes) → full 92-route puppeteer hydration check = **ALL 92 CLEAN, 0 #418/hydration errors**; production-served build screenshot of an article + related section = 0 page errors, correct rendering/spacing; 0 `&amp;#39;` remain anywhere. Repackaged `/app/original-site` (92 HTML pages incl. root + PHP `/api` preserved) + archive `BestBuyIncentives-ORIGINAL-DESIGN-DELIVERABLE.tgz` = `d35d276a02745760a5168c8c1f4473d27388b2d3a08ffb9a7aee214e97c89268` (~99MB, 92 index.html, sitemap 92 URLs). Updated `DEPLOY_THIS_ORIGINAL_SITE.txt` (new hash, 92 pages, +72 articles). NOTE: user CONFIRMED they DO want this pushed to GitHub (earlier "never push" was only to prevent autonomous pushing) — push is done by the user via the "Save to GitHub" button; agent cannot push directly.
- P1: Post-launch — verify live SEO audit passes on bestbuyincentives.com; submit sitemap to Google/Bing Search Console.
- P1: Confirm real Calendly booking URL.
- P2: Consider self-hosting Google Fonts to remove the one remaining external font request (not Emergent; kept to avoid visual change).
